// config.example.js — safe to commit, no real credentials.
// Copy this to config.js and fill in your values.
const CONFIG = {
  clientId: 'YOUR_CLIENT_ID.apps.googleusercontent.com',
  sheetId:  'YOUR_SPREADSHEET_ID',
};
